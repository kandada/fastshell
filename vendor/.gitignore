# (2026-07) The old CPython *.so/*.dylib ignore rules were removed together
# with the Chaquopy integration. libffi/**/libffi.a MUST be committed — it is
# required for the Android build (rustpython ctypes; the NDK ships no libffi).
.DS_Store
.a